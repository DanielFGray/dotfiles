#!/usr/bin/env bash

if [[ -e ${HOME}/.bash_utils ]]; then
  source ${HOME}/.bash_utils
else
  echo -e "\e[31mbash_utils not found\e[0m" >&2
  exit 1
fi

favs=('urxvtcd' 'urxvtc' 'urxvt' 'xterm')
cmd=$(getCommandFallbacks ${favs[*]})
( exec $cmd "$@" ) &
